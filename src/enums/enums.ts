export enum Timeout {
	Short = 2500,
	Medium = 5000,
	Long = 10000,
}

export enum Status {
	Success = 'success',
	Error = 'error',
	Warning = 'warning',
	Info = 'info',
	Idle = 'idle',
	Failed = 'failed',
	Loading = 'loading',
}

export enum Role {
	User = 'User',
	Admin = 'Admin',
}
